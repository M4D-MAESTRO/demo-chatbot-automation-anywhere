import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.standforthewin.plataform',
  appName: 'Plataform',
  webDir: 'www',
  bundledWebRuntime: false,
  
};

export default config;
